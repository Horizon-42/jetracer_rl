# --- 核心算法库 ---
stable-baselines3[extra]>=2.3.0

# --- 强化学习标准接口 ---
# Shimmy 充当 "翻译官"，让新版 Gymnasium 能读懂旧版 Gym 环境
gymnasium>=0.29.1
shimmy>=1.3.0

# --- 必要的旧版 Gym ---
# DonkeyCar 是基于旧 Gym 开发的，Shimmy 需要这个版本作为底层支持
# 注意：setuptools 版本过高可能会导致 gym 0.21 安装失败，如果报错请降级 setuptools<60
# gym==0.21.0 

# --- 图像处理 ---
opencv-python==4.9.0.80
pillow

# --- 数学库 ---
numpy<2.0.0

# --- PyTorch (通常 SB3 会自动安装，但显式指定更安全) ---
# 如果你是 Windows/Linux 且有 NVIDIA 显卡，建议去 PyTorch 官网找对应 CUDA 版本的安装命令
# 这里写的是通用的 cpu/gpu 自动版，稍后建议用专用命令覆盖
torch>=2.0.0

